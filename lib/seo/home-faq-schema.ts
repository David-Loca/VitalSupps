import type { Locale } from "@/lib/i18n";

/** Homepage FAQPage schema entities — currently no locale-specific override; layout.tsx supplies the default FAQ. */
export function getHomeFaqMainEntity(_locale: Locale) {
  return null;
}
